{
  "name": "deepak640-url-shortener",
  "version": "1.0.0",
  "description": "CLI tool for shortening URLs with a fixed Vercel backend and .env support.",
  "main": "run.js",
  "bin": {
    "urlshortner": "run.js"
  },
  "scripts": {
    "postinstall": "node install.js",
    "build-all": "GOOS=darwin GOARCH=amd64 go build -o bin/urlshortner-darwin-amd64 . && GOOS=darwin GOARCH=arm64 go build -o bin/urlshortner-darwin-arm64 . && GOOS=linux GOARCH=amd64 go build -o bin/urlshortner-linux-amd64 . && GOOS=windows GOARCH=amd64 go build -o bin/urlshortner-windows-amd64.exe .",
    "deploy": "npm run build-all && npm version patch && npm publish --access public"
  },
  "files": [
    "bin/",
    "run.js",
    "install.js"
  ],
  "author": "Deepak Negi",
  "license": "MIT",
  "keywords": [
    "url",
    "shortener",
    "cli",
    "go"
  ]
}
